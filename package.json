{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Teacher Grading System - Quiz with Scoreboard",
  "scripts": {
    "start": "npx http-server -p 8080",
    "build": "npx @tailwindcss/cli -i ./src/styles.css -o ./dist/output.css",
    "watch": "npx @tailwindcss/cli -i ./src/styles.css -o ./dist/output.css --watch"
  }
}
